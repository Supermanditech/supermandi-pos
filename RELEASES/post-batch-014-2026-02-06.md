# POST-BATCH-014: Production-Grade Fixes

**Date:** 2026-02-06
**Base SHA:** `dac5e25` (main, includes BATCH-014 + SUP-ROOT-001)
**Commit SHA:** `0b4a395`
**Tag:** `pos-build-2026-02-06_1730IST`

---

## Tickets Implemented (5)

| # | Ticket | Priority | Status | Evidence |
|---|--------|----------|--------|----------|
| 1 | BACKEND-CRASH-001 | P0 | FIXED | POST /admin/stores → 500 (was: process crash). Backend stays alive after 3× repeated calls |
| 2 | DOCKER-RESTART-001 | P0 | FIXED | 17/17 containers: `RestartPolicy = unless-stopped` (verified via `docker inspect`) |
| 3 | DOCKER-ENV-001 | P1 | FIXED | Resend key + Firebase project ID injected via `scripts/.env`. OTP email delivered to `supermanditech@gmail.com`, verified end-to-end |
| 4 | DOCKER-BUILD-001 | P2 | FIXED | main-backend built from `Dockerfile.main` (was: stale image `:2d7189d`). Image tag: `supermandi/main-backend:local` |
| 5 | SUP-REG-001 | P1 | FIXED | Missing GSTIN → 400. Invalid GSTIN → 400. Valid GSTIN → 201 with `verification_status = KYC_SUBMITTED` |

## Sub-fixes (discovered during implementation)

| # | Sub-fix | Root Cause | Fix |
|---|---------|-----------|-----|
| 1 | EMAIL_FROM domain | Resend verified domain is `supermandi.tech`, code defaulted to `supermandi.com` | Added `EMAIL_FROM: "SuperMandi <noreply@supermandi.tech>"` to docker-compose |
| 2 | Firebase SA volume mount | Docker creates directory (not file) when host path missing → Node `require()` fails differently | Removed volume mount; use `FIREBASE_PROJECT_ID` + ADC instead |

---

## Files Changed

| File | Ticket | Change |
|------|--------|--------|
| `backend/src/routes/v1/admin/stores.ts` | BACKEND-CRASH-001 | Replace `throw err` with `console.error` + `res.status(500)` in catch block |
| `backend/src/routes/v1/supplier/auth.ts` | SUP-REG-001 | Add GSTIN required validation, change `'pending'` → `'KYC_SUBMITTED'`, remove null fallback |
| `scripts/docker-compose.local-prod.yml` | DOCKER-BUILD-001, DOCKER-ENV-001, DOCKER-RESTART-001 | Build main-backend from source, add env vars (Resend/Firebase/EMAIL_FROM), add `restart: unless-stopped` to all 17 services |
| `.gitignore` | DOCKER-ENV-001 | Add `firebase-service-account*.json` + `*-firebase-adminsdk-*.json` patterns |
| `scripts/.env` *(gitignored)* | DOCKER-ENV-001 | Created with `RESEND_API_KEY` + `FIREBASE_PROJECT_ID` (secrets, never committed) |

## Build Artifacts Untracked

10 `*.tsbuildinfo` files removed from git tracking (already in `.gitignore` as `**/*.tsbuildinfo`):
- `backend/tsconfig.tsbuildinfo`
- `backend/packages/common/tsconfig.tsbuildinfo`
- `backend/services/{api-gateway,auth-service,catalog-service,inventory-service,order-service,platform-service,reorder-service,supplier-service}/tsconfig.tsbuildinfo`

---

## Before → After

| Test | Before (dac5e25) | After (this commit) |
|------|-------------------|---------------------|
| POST /admin/stores (bad data) | **Process crash** (unhandled throw) | 500 `{"error":"store_creation_failed"}`, backend stays alive |
| Backend health after crash test | **Container restart loop** | 200 `{"status":"ok"}` — no restart needed |
| POST /supplier/auth/register (no GSTIN) | **500** (NOT NULL violation) | 400 `{"error":{"code":"VALIDATION_ERROR","message":"GSTIN is required..."}}` |
| POST /supplier/auth/register (invalid GSTIN) | **500** (check constraint) | 400 `{"error":{"code":"VALIDATION_ERROR","message":"Invalid GSTIN format"}}` |
| POST /supplier/auth/register (valid) | **500** (`'pending'` not in enum) | 201 with `verification_status: KYC_SUBMITTED` |
| Docker restart policy | **Not set** (default: `no`) | `unless-stopped` on all 17 containers |
| main-backend image | Stale `:2d7189d` (4 commits behind) | Built from current source via `Dockerfile.main` |
| Admin OTP email delivery | **Failed** (no Resend key, wrong FROM domain) | Delivered to `supermanditech@gmail.com` via Resend (verified with real OTP `619234`) |
| Firebase init | **Failed** (SA file not found) | Initialized with project `supermandi-pos` via ADC |

---

## Smoke Test Results (2026-02-06)

### Phase 0: Access Paths (6/6 PASS)

| Endpoint | Port | Status |
|----------|------|--------|
| API Gateway | 8080 | 200 |
| Main Backend | 3010 | 200 |
| Retailer Admin | 8081 | 200 |
| Supplier Portal | 8082 | 200 (via /supplier/ redirect) |
| SuperAdmin | 8083 | 200 |
| Landing | 8084 | 200 |

### Phase 1: Functional Tests (all PASS)

| Test | Result |
|------|--------|
| BACKEND-CRASH-001: Store creation → 500, backend alive | PASS |
| SUP-REG-001: No GSTIN → 400 | PASS |
| SUP-REG-001: Invalid GSTIN → 400 | PASS |
| DOCKER-RESTART-001: 17/17 unless-stopped | PASS |
| Admin OTP send → Resend delivery | PASS |
| Admin OTP verify → JWT issued (role: super_admin) | PASS |

### Container Status: 17/17 Healthy

```
scripts-api-gateway-1         Up (healthy)
scripts-auth-service-1        Up (healthy)
scripts-catalog-service-1     Up (healthy)
scripts-inventory-service-1   Up (healthy)
scripts-landing-1             Up (healthy)
scripts-main-backend-1        Up (healthy)
scripts-order-service-1       Up (healthy)
scripts-payment-service-1     Up (healthy)
scripts-platform-service-1    Up (healthy)
scripts-postgres-1            Up (healthy)
scripts-redis-1               Up (healthy)
scripts-reorder-service-1     Up (healthy)
scripts-retailer-admin-1      Up (healthy)
scripts-superadmin-1          Up (healthy)
scripts-supplier-portal-1     Up (healthy)
scripts-supplier-service-1    Up (healthy)
scripts-voice-service-1       Up (healthy)
```

---

## Gate Results

| Gate | Result |
|------|--------|
| `pnpm -r typecheck` | 0 errors (22/22 projects) |
| Docker build (main-backend + supplier-portal) | Success |
| Local prod stack | 17/17 healthy |
| Secrets in git | None (`scripts/.env` gitignored, Firebase SA patterns gitignored) |

---

## Known Limitations (not in scope)

1. **Store creation returns 500** (not crash): The underlying `generate_store_code()` DB function or column defaults need a separate schema fix ticket
2. **Supplier Firebase-login register path** (`auth.ts:1422`): Still has `gstin?.toUpperCase() || null` — needs follow-up ticket for the Firebase auth registration path
3. **Firebase SA file**: Using ADC (Application Default Credentials) — full SA file auth deferred until operator provides the JSON

---

## POST-BATCH-015: Stability Retest (Cold Start)

**Method:** `docker compose down` (full teardown) → `docker compose up -d` (cold start from tag `pos-build-2026-02-06_1730IST`)

### Cold Start Result
- All 17 containers: **healthy** within 60 seconds
- No restart loops, no error exits

### Phase 0 Retest (6/6 PASS — identical to prior run)

| Endpoint | Port | Status | Match |
|----------|------|--------|-------|
| API Gateway | 8080 | 200 | identical |
| Main Backend | 3010 | 200 | identical |
| Retailer Admin | 8081 | 200 | identical |
| Supplier Portal | 8082 | 200 | identical |
| SuperAdmin | 8083 | 200 | identical |
| Landing | 8084 | 200 | identical |

### Phase 1 Retest (6/6 PASS — identical to prior run)

| Test | Result | Match |
|------|--------|-------|
| BACKEND-CRASH-001: Store creation → `{"error":"store_creation_failed"}` | PASS | identical |
| Backend health after crash test → 200 | PASS | identical |
| SUP-REG-001: No GSTIN → 400 `VALIDATION_ERROR` | PASS | identical |
| SUP-REG-001: Invalid GSTIN → 400 `Invalid GSTIN format` | PASS | identical |
| DOCKER-RESTART-001: `unless-stopped` on all containers | PASS | identical |
| Admin OTP send → `{"success":true}` via Resend | PASS | identical |

### Stability Verdict

**NO FLAKINESS DETECTED.** All results from cold start are byte-identical to warm-state results. The tag `pos-build-2026-02-06_1730IST` is stable for staging handoff.
